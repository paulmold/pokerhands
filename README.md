## Requirements

- Composer

## How to install

Run these commands:

`git clone git@github.com:paulmold/pokerhands.git`

`cd pokerhands`

`cp .env.example .env`

`composer install`

`php artisan migrate`
